from window import Window

# Screen and grid constants
SCREEN_WIDTH = 1200
SCREEN_HEIGHT = 900

## Main
if __name__ == '__main__':
    wh_planner = Window(SCREEN_WIDTH, SCREEN_HEIGHT)
    wh_planner.run()
